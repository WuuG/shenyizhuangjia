import { PhonFormatDirective } from './phon-format.directive';

describe('PhonFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new PhonFormatDirective();
    expect(directive).toBeTruthy();
  });
});
